{
  "name": "mockbase",
  "version": "0.4.2",
  "description": "Firebase v4+ mock.",
  "main": "app/index.js",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^10.14.18",
    "coveralls": "^3.0.6",
    "firebase": "^6.6.1",
    "jest": "^24.9.0",
    "jest-create-mock-instance": "^1.1.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.3"
  },
  "scripts": {
    "build": "tsc",
    "clean": "git clean -Xf **/*.js **/*.js.map **/*.d.ts",
    "prepublish": "npm run build",
    "test": "jest",
    "test:watch": "jest --watch --notify"
  },
  "repository": "gustavohenke/firebase-stub",
  "license": "MIT",
  "jest": {
    "testEnvironment": "node",
    "collectCoverage": true,
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.ts$",
    "moduleFileExtensions": [
      "ts",
      "js",
      "json",
      "node"
    ]
  }
}
